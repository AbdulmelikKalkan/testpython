from setuptools import setup, find_packages

setup(
    name='greetings',  # Replace with your desired library name
    version='1.0.0',  # Replace with your library version
    packages=find_packages(),
    description='A simple greetings library',  # Add a description
)
